micronaut:
  application:
    name: workshop-ssm-admin
  config-client:
    enabled: true
aws:
  client:
    system-manager:
      parameterstore:
        enabled: true
        root-hierarchy-path: "/config/"